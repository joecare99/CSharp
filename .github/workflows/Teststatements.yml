name: TestStatemens CI0

on:
  push:
    paths:
      - "TestStatements/**"
  pull_request:
    paths:
      - "TestStatements/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: TestStatemens
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Remove private projects
        run: |
          
      - name: Restore
        run: dotnet restore TestStatemens.sln -p:TargetFramework=net8.0 -p:EnableWindowsTargeting=true

      - name: Build
        run: dotnet build TestStatemens.sln --configuration Release --no-restore -p:TargetFramework=net8.0 -p:EnableWindowsTargeting=true

      - name: Test
        run: dotnet test TestStatemens.sln --configuration Release --no-build --verbosity normal -p:TargetFramework=net8.0 -p:EnableWindowsTargeting=true
