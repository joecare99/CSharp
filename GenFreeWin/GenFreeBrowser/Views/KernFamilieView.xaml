<UserControl x:Class="GenFreeBrowser.Views.KernFamilieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ctrls="clr-namespace:GenFreeBrowser.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="900">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="Bool2Visibility" />
        <Style x:Key="SectionBox" TargetType="GroupBox">
            <Setter Property="Margin" Value="0 6 0 0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Selected person block with parents header inside summary control -->
        <GroupBox Header="Person" Grid.Row="0" Grid.Column="0" Style="{StaticResource SectionBox}">
            <ctrls:PersonSummaryControl
                ParentsHeader="{Binding ParentsHeader}"
                DisplayName="{Binding AusgewaehltePerson.Vollname}"
                VitalData="{Binding SelectedVital}" />
        </GroupBox>

        <!-- Partner block -->
        <GroupBox Header="Partner" Grid.Row="0" Grid.Column="1" Style="{StaticResource SectionBox}"
                  Visibility="{Binding HatEhePartner, Converter={StaticResource Bool2Visibility}}">
            <ctrls:PersonSummaryControl
							    ParentsHeader="{Binding ParentsHeader2}"
							    DisplayName="{Binding EhePartner.Vollname}"
                  VitalData="{Binding PartnerVital}" />
        </GroupBox>

        <!-- Children table -->
        <GroupBox Header="Kinder" Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource SectionBox}"
                  Visibility="{Binding HatKinder, Converter={StaticResource Bool2Visibility}}">
            <DataGrid ItemsSource="{Binding Kinder}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Vollname}" Width="*"/>
                    <DataGridTextColumn Header="Geburt" Binding="{Binding GeburtsDatum}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>