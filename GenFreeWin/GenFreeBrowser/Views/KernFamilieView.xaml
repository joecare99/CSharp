<UserControl x:Class="GenFreeBrowser.Views.KernFamilieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="Bool2Visibility" />
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Header="Person" Margin="0 0 0 8">
            <StackPanel>
                <TextBlock Text="Name:" FontWeight="Bold" />
                <TextBlock Text="{Binding AusgewaehltePerson.Vollname}" />
                <TextBlock Text="Geburt:" FontWeight="Bold" Margin="0 8 0 0" />
                <TextBlock Text="{Binding AusgewaehltePerson.GeburtsDatum}" />
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Ehepartner" Margin="0 0 0 8" Grid.Row="1" Visibility="{Binding HatEhePartner, Converter={StaticResource Bool2Visibility}}">
            <StackPanel>
                <TextBlock Text="{Binding EhePartner.Vollname}" />
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Kinder" Grid.Row="2" Visibility="{Binding HatKinder, Converter={StaticResource Bool2Visibility}}">
            <ItemsControl ItemsSource="{Binding Kinder}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Vollname}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>
    </Grid>
</UserControl>