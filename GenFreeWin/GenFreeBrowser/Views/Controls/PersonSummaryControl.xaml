<UserControl x:Class="GenFreeBrowser.Views.Controls.PersonSummaryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="180" d:DesignHeight="120">
    <UserControl.Resources>
        <!-- Fallback Silhouette as DrawingImage -->
        <DrawingImage x:Key="FallbackSilhouette">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#FFC0C8D0">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry RadiusX="28" RadiusY="28" Center="28,28" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#FF9AA3AD">
                        <GeometryDrawing.Geometry>
                            <!-- Simple torso shape -->
                            <PathGeometry Figures="M4,80 C4,60 20,48 28,48 36,48 52,60 52,80 Z"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>
    </UserControl.Resources>
    <Border CornerRadius="6" Padding="6" Background="#FFF8F9FB" BorderBrush="#FFCED4DA" BorderThickness="1">
        <DockPanel>
            <Border Width="56" Height="56" CornerRadius="4" Background="#FFEFF2F5" Margin="0 0 8 0" DockPanel.Dock="Left">
                <Image x:Name="PersonImage" Stretch="UniformToFill" />
            </Border>
            <StackPanel>
                <TextBlock Text="{Binding ElementName=root, Path=DisplayName}" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding ElementName=root, Path=VitalData}" FontSize="11" Foreground="#FF555B66" />
                <TextBlock Text="{Binding ElementName=root, Path=AdditionalInfo}" FontSize="10" Foreground="#FF777F88" TextWrapping="Wrap" />
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>