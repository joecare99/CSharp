<UserControl x:Class="GenFreeBrowser.Views.Controls.PersonCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="150" d:DesignHeight="80"
             Width="150" Height="80">
    <Border CornerRadius="6" Padding="6" Background="#FFF8F9FB" BorderBrush="#FFCED4DA" BorderThickness="1">
        <DockPanel>
            <Border Width="32" Height="32" CornerRadius="3" Background="#FFEFF2F5" Margin="0 0 6 0" DockPanel.Dock="Left">
                <Image x:Name="Img" Source="{Binding Photo, RelativeSource={RelativeSource AncestorType=UserControl}}" Stretch="UniformToFill" />
            </Border>
            <StackPanel>
                <TextBlock Text="{Binding DisplayName, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="Bold" FontSize="12" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="10" Foreground="#FF555B66"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
