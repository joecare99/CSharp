# AppKomponentBaseLib

Reusable application-component contracts that are broader than AA98 but higher-level than BaseLib.

The shared command layer now includes tool-capable command metadata for AI-assisted workflows, including parameter and result descriptors, execution context requirements, safety levels, and consent metadata.
