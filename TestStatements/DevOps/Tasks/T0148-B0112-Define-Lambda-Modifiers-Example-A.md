# T0148 - Define Lambda Modifiers Example A

## Parent Backlog Item

- B0112 - Define C# 14 Feature Comparison Scenarios

## Summary

Define the first lambda-modifiers comparison example for a simple delegate with an `out` parameter where C# 14 can omit explicit parameter types.

## Scope

- modern form using a lambda parameter modifier without explicit parameter types
- older alternative using an explicitly typed lambda parameter list
- exact comparison with equivalent delegate behavior

## Example Direction

- use a small parsing-style delegate with `out`
- keep the example compact and deterministic
- emphasize reduced syntax noise without changing semantics

## Done Criteria

- The comparison focus is fixed.
- The example is ready for a detailed baseline.
