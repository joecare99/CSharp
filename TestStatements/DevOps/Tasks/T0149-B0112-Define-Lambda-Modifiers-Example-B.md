# T0149 - Define Lambda Modifiers Example B

## Parent Backlog Item

- B0112 - Define C# 14 Feature Comparison Scenarios

## Summary

Define the second lambda-modifiers comparison example for `ref`, `in`, or `ref readonly` parameters in a realistic delegate scenario.

## Scope

- modern form using lambda parameter modifiers with omitted types where allowed
- older alternative using an explicitly typed lambda parameter list
- exact comparison with equivalent delegate behavior

## Example Direction

- use a small delegate where by-reference intent is visible
- keep the example minimal and easy to scan
- focus on readability and ceremony reduction

## Done Criteria

- The comparison focus is fixed.
- The example is ready for a detailed baseline.
