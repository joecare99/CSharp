# T0150 - Define Lambda Modifiers Example C

## Parent Backlog Item

- B0112 - Define C# 14 Feature Comparison Scenarios

## Summary

Define the third lambda-modifiers comparison example for boundary cases such as `params` and modifier-related readability limits.

## Scope

- modern form using omitted parameter types with modifiers where allowed
- older alternative using an explicitly typed lambda parameter list
- caveat notes for cases where explicit types are still required, especially `params`

## Example Direction

- show one supported modifier-focused case and one explicit boundary note
- keep the main comparison exact where possible
- document restrictions instead of hiding them

## Done Criteria

- The comparison focus is fixed.
- The limitation notes are explicit.
- The example is ready for a detailed baseline.
