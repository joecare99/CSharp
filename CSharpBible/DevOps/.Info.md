# DevOps Info

## Scope
Refactor common test item behavior in `Games/Game_BaseTests/Model` into a shared base class while keeping existing concrete test types and test expectations stable.

## Current Task
- Introduce a common base class for `TestItem` and `TestItem2D`
- Preserve type-specific logging events and string formatting
- Validate with relevant MSTest test classes

## Next Task
- Migrate `MVVM_Tutorial/UWP_00_Test/UWP_00_Test.csproj` to `net10.0-windows10.0.19041.0`
- Migrate `App2/App2.csproj` to `net10.0-windows10.0.19041.0`
- Fix the default platform in `App2` so builds no longer fall back to `AnyCPU`

## Additional Active Task
- Start the Roslyn builder starter slice under `Data/Workbench.Builder`
- Create `Workbench.Builder.Core` and `Workbench.Builder.Core.Tests`
- Prepare V1.1 inspection models and contracts for later Workbench integration
