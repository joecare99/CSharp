<Window
    x:Class="Galaxia.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Galaxia.UI.ViewModels"
    Title="Galaxia"
    Width="900"
    Height="600"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <DockPanel>
        <StackPanel
            Margin="4"
            DockPanel.Dock="Top"
            Orientation="Horizontal">
            <Button
                Margin="2"
                Command="{Binding InitializeCommand}"
                Content="Initialisieren" />
            <Button
                Margin="2"
                Command="{Binding EmbarkTestFleetCommand}"
                Content="Embark Testflotte"
                ToolTip="Legt eine Test-Flotte in einen HyperSlot" />
            <Button
                Margin="2"
                Command="{Binding DoubleJumpDemoCommand}"
                Content="Double Jump Demo" />
            <TextBlock
                Margin="16,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding Status}" />
        </StackPanel>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Sectors}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Window>