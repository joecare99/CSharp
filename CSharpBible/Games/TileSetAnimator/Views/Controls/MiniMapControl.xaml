<UserControl x:Class="TileSetAnimator.Views.Controls.MiniMapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:TileSetAnimator.Converters"
             xmlns:p="clr-namespace:TileSetAnimator.Properties"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="300">
        <UserControl.Resources>
        <conv:CroppedBitmapConverter x:Key="CroppedBitmapConverter" />
        <conv:NullToBooleanConverter x:Key="NullToBooleanConverter" />
        <Style TargetType="ListBoxItem" x:Key="MiniMapSlotItemStyle">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="2" />
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="{StaticResource TileBorderBrush}" BorderThickness="1" Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBlock Text="{x:Static p:Resources.MiniMapSelectionLabel}" />
                    <ComboBox ItemsSource="{Binding MiniMaps}"
                              SelectedItem="{Binding SelectedMiniMap}"
                              DisplayMemberPath="Name"
                              Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,8,0,0">
                    <TextBlock Text="{x:Static p:Resources.MiniMapNameLabel}" />
                    <TextBox Margin="0,4,0,0"
                             Text="{Binding SelectedMiniMap.Name, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding SelectedMiniMap, Converter={StaticResource NullToBooleanConverter}}" />
                </StackPanel>
                <WrapPanel Grid.Row="2" ItemWidth="140">
                    <Button Content="{x:Static p:Resources.MiniMapAddButtonText}"
                            Command="{Binding CreateMiniMapCommand}" Margin="0,4,4,4" />
                    <Button Content="{x:Static p:Resources.MiniMapRemoveButtonText}"
                            Command="{Binding RemoveMiniMapCommand}" Margin="0,4,4,4"
                             />
                </WrapPanel>
            </Grid>

            <ListBox Grid.Row="1"
                     Margin="0,0,0,8"
                     ItemsSource="{Binding MiniMapSlots}"
                     SelectedItem="{Binding SelectedMiniMapSlot}"
                     ItemContainerStyle="{StaticResource MiniMapSlotItemStyle}"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="5" Columns="5" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{StaticResource TileBorderBrush}"
                                BorderThickness="1"
                                Padding="2">
                            <Grid>
                                <Image x:Name="TilePreview"
                                       Stretch="Fill"
                                       ToolTip="{Binding Tile.Name}">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tile}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                    <Image.Source>
                                        <MultiBinding Converter="{StaticResource CroppedBitmapConverter}">
                                            <Binding Path="DataContext.TileSheet" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                            <Binding Path="Tile.Bounds" />
                                        </MultiBinding>
                                    </Image.Source>
                                </Image>
                                <TextBlock Text="{Binding DisplayLabel}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="Gray">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tile}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="2"
                        Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBlock Text="{x:Static p:Resources.MiniMapSuggestedSlotLabel}" />
                    <ComboBox Margin="0,4,0,0"
                              ItemsSource="{Binding DataContext.MiniMapSuggestionOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                              DataContext="{Binding SelectedMiniMapSlot}"
                              SelectedItem="{Binding SuggestionKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              IsEnabled="{Binding Converter={StaticResource NullToBooleanConverter}}" />
                </StackPanel>
                <WrapPanel Grid.Row="1" Margin="0,4,0,0" ItemWidth="140" HorizontalAlignment="Center">
                    <Button Content="{x:Static p:Resources.MiniMapPlaceTileButtonText}"
                            Command="{Binding PlaceTileInMiniMapCommand}" Margin="0,4,4,4" />
                    <Button Content="{x:Static p:Resources.MiniMapApplySuggestionButtonText}"
                            Command="{Binding UseSuggestedTileInMiniMapCommand}" Margin="0,4,5,4" />
                </WrapPanel>
            </Grid>

            <StackPanel Grid.Row="3"
                        Margin="0,8,0,0"
                        Orientation="Horizontal">
                <Button Content="{x:Static p:Resources.MiniMapClearSlotButtonText}"
                        Command="{Binding ClearMiniMapSlotCommand}" />
                <Button Content="{x:Static p:Resources.MiniMapClearButtonText}"
                        Command="{Binding ClearMiniMapCommand}"
                        Margin="8,4,0,4" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
