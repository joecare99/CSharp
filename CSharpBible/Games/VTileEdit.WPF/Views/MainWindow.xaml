<Window x:Class="VTileEdit.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:VTileEdit.WPF.Properties"
        xmlns:vm="clr-namespace:VTileEdit.WPF.ViewModels"
        mc:Ignorable="d"
        Title="{x:Static properties:Strings.MainWindowTitle}"
        Height="720"
        Width="1200"
        MinWidth="960"
        MinHeight="640">
    <Window.Resources>
        <Style x:Key="GlyphButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
        </Style>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="{x:Static properties:Strings.MenuFileHeader}">
                <MenuItem Header="{x:Static properties:Strings.MenuFileNew}" Command="{Binding NewCommand}" />
                <MenuItem Header="{x:Static properties:Strings.MenuFileOpen}" Command="{Binding OpenCommand}" />
                <Separator />
                <MenuItem Header="{x:Static properties:Strings.MenuFileSave}" Command="{Binding SaveCommand}" />
                <MenuItem Header="{x:Static properties:Strings.MenuFileSaveAs}" Command="{Binding SaveAsCommand}" />
                <Separator />
                <MenuItem Header="{x:Static properties:Strings.MenuFileExit}" Command="{Binding ExitCommand}" />
            </MenuItem>
            <MenuItem Header="{x:Static properties:Strings.MenuEditHeader}">
                <MenuItem Header="{x:Static properties:Strings.MenuEditUndo}" Command="{Binding UndoCommand}" />
                <MenuItem Header="{x:Static properties:Strings.MenuEditRedo}" Command="{Binding RedoCommand}" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="{x:Static properties:Strings.TileListHeader}" Margin="5">
                <DockPanel>
                    <TextBlock Text="{x:Static properties:Strings.TileListHint}" Margin="0,0,0,8" Foreground="Gray" />
                    <ListBox ItemsSource="{Binding Tiles}"
                             SelectedItem="{Binding SelectedTile}"
                             DockPanel.Dock="Bottom">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:TileViewModel}">
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" />
                                    <ItemsControl ItemsSource="{Binding Glyphs}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Columns="{Binding TileWidth}" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:GlyphCellViewModel}">
                                                <Border Background="{Binding BackgroundBrush}" BorderBrush="#22000000" BorderThickness="0.5">
                                                    <TextBlock Text="{Binding Character}" Foreground="{Binding ForegroundBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas" FontSize="10" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </GroupBox>
            <Grid Grid.Column="2" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <GroupBox Header="{x:Static properties:Strings.PreviewHeader}" Margin="0,0,0,8">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedTile.Glyphs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="{Binding SelectedTile.TileWidth}" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:GlyphCellViewModel}">
                                    <Border Background="{Binding BackgroundBrush}" BorderThickness="0.5" BorderBrush="#22000000" Margin="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="#22000000" />
                                                <Setter Property="BorderThickness" Value="0.5" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#FF5A9BD8" />
                                                        <Setter Property="BorderThickness" Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Button Style="{StaticResource GlyphButtonStyle}"
                                                Command="{Binding DataContext.SelectGlyphCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}">
                                            <TextBlock Text="{Binding Character}" FontFamily="Consolas" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{Binding ForegroundBrush}" />
                                        </Button>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </GroupBox>
                <GroupBox Header="{x:Static properties:Strings.GlyphDetailsHeader}" Grid.Row="1" Margin="0,0,0,8">
                    <Grid Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="{x:Static properties:Strings.GlyphCharacterLabel}" Margin="0,0,12,8" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" Text="{Binding SelectedGlyph.CharacterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" FontSize="16" HorizontalContentAlignment="Center" />
                        <TextBlock Grid.Row="1" Text="{x:Static properties:Strings.GlyphForegroundLabel}" Margin="0,0,12,8" VerticalAlignment="Center" />
                        <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Palette}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedForegroundSwatch}" />
                        <TextBlock Grid.Row="2" Text="{x:Static properties:Strings.GlyphBackgroundLabel}" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Palette}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedBackgroundSwatch}" />
                    </Grid>
                </GroupBox>
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <GroupBox Header="{x:Static properties:Strings.PaletteForegroundHeader}" Margin="0,0,4,0">
                        <ItemsControl ItemsSource="{Binding Palette}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ColorSwatchViewModel}">
                                    <ToggleButton IsChecked="{Binding IsForegroundSelection, Mode=TwoWay}"
                                                  Command="{Binding DataContext.ApplyForegroundCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}"
                                                  Margin="2"
                                                  Width="50"
                                                  Height="30">
                                        <Border Background="{Binding Brush}" CornerRadius="4" />
                                    </ToggleButton>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                    <GroupBox Header="{x:Static properties:Strings.PaletteBackgroundHeader}" Grid.Column="1" Margin="4,0,0,0">
                        <ItemsControl ItemsSource="{Binding Palette}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ColorSwatchViewModel}">
                                    <ToggleButton IsChecked="{Binding IsBackgroundSelection, Mode=TwoWay}"
                                                  Command="{Binding DataContext.ApplyBackgroundCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}"
                                                  Margin="2"
                                                  Width="50"
                                                  Height="30">
                                        <Border Background="{Binding Brush}" CornerRadius="4" />
                                    </ToggleButton>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </GroupBox>
                </Grid>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
