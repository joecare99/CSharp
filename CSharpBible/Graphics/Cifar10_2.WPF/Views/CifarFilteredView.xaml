<Page
    x:Class="Cifar10.WPF.Views.CifarFilteredView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Cifar10.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="CifarFilteredView"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="Image">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="2" />
        </Style>
    </Page.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Command="{Binding LoadDataCommand}" Header="Load CIFAR Data" />
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="5" />
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Column="0">
                    <Label DockPanel.Dock="Top">Original</Label>
                    <Image
                        Width="128"
                        Height="128"
                        Source="{Binding OrginalImage}"
                        Stretch="Uniform" />
                </DockPanel>
                <DockPanel Grid.Column="1">
                    <Label DockPanel.Dock="Top">Low-Res Input</Label>
                    <Image
                        Width="128"
                        Height="128"
                        Source="{Binding DestImage}"
                        Stretch="Uniform" />

                </DockPanel>
                <DockPanel Grid.Column="2">
                    <Label DockPanel.Dock="Top">Upscaled (ML.NET)</Label>
                    <Image
                        Width="128"
                        Height="128"
                        Source="{Binding GenResultImage}"
                        Stretch="Uniform" />

                </DockPanel>

                <ScrollViewer
                    Grid.Row="1"
                    Grid.ColumnSpan="3"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Images}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Image
                                    MaxWidth="64"
                                    Source="{Binding}"
                                    Stretch="Uniform" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
            <StackPanel>
                <Button
                    Height="42"
                    Command="{Binding ProcImagesCommand}"
                    Content="Train &amp; Upscale" />
                <TextBlock Margin="0,6,0,0" Text="{Binding Accuracy, StringFormat=R²: {0:F3}}" />
                <TextBlock
                    Margin="0,4,0,0"
                    Text="{Binding StatusText}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Page>
