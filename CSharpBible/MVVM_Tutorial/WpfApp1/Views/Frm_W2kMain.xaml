<!--  Generated from LFM file  -->
<Window
    x:Class="WpfApp.Views.Frm_W2kMain"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Client-Anwendung für Windows 2000"
    Width="466"
    Height="308"
    Background="#F0F0F0"
    WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem x:Name="File1" Header="_amp;Datei">
                <MenuItem
                    x:Name="FileNewItem"
                    Header="_amp;Neu"
                    InputGestureText="Ctrl+N" />
                <MenuItem
                    x:Name="FileOpenItem"
                    Header="Ö_amp;ffnen"
                    InputGestureText="Ctrl+O" />
                <MenuItem
                    x:Name="FileSaveItem"
                    Header="_amp;Speichern"
                    InputGestureText="Ctrl+S" />
                <MenuItem x:Name="FileSaveAsItem" Header="Speichern _amp;unter..." />
                <Separator />
                <MenuItem x:Name="FileSendItem" Header="Sen_amp;den" />
                <Separator />
                <MenuItem x:Name="FileExitItem" Header="_amp;Beenden" />
            </MenuItem>
            <MenuItem x:Name="Edit1" Header="_amp;Bearbeiten">
                <MenuItem
                    x:Name="CutItem"
                    Header="_amp;Ausschneiden"
                    InputGestureText="Ctrl+X" />
                <MenuItem
                    x:Name="CopyItem"
                    Header="_amp;Kopieren"
                    InputGestureText="Ctrl+C" />
                <MenuItem
                    x:Name="PasteItem"
                    Header="_amp;Einfügen"
                    InputGestureText="Ctrl+V" />
            </MenuItem>
            <MenuItem x:Name="Help1" Header="_amp;Hilfe">
                <MenuItem x:Name="HelpAboutItem" Header="_amp;Info..." />
            </MenuItem>
        </Menu>
        <Canvas>
            <StatusBar
                x:Name="StatusBar"
                Canvas.Left="0"
                Canvas.Top="246"
                Width="450"
                Height="23" />
            <ToolBar
                x:Name="ToolBar1"
                Canvas.Left="0"
                Canvas.Top="0"
                Width="450"
                Height="24">
                <!--  ImageIndex: 6  -->
                <Button x:Name="ToolButton1" ToolTip="Neu|Neue Datei erstellen" />
                <!--  ImageIndex: 7  -->
                <Button x:Name="ToolButton2" ToolTip="Öffnen|Datei öffnen" />
                <!--  ImageIndex: 8  -->
                <Button x:Name="ToolButton3" ToolTip="Speichern|Aktuelle Datei speichern" />
                <Separator />
                <!--  ImageIndex: 15  -->
                <Button x:Name="ToolButton5" ToolTip="Senden|Aktuelle Datei per E-Mail verschicken" />
                <Separator />
                <!--  ImageIndex: 0  -->
                <Button x:Name="ToolButton7" ToolTip="Ausschneiden|Markiertes Objekt in die Zwischenablage verschieben" />
                <!--  ImageIndex: 1  -->
                <Button x:Name="ToolButton8" ToolTip="Kopieren|Markiertes Objekt in die Zwischenablage kopieren" />
                <!--  ImageIndex: 2  -->
                <Button x:Name="ToolButton9" ToolTip="Einfügen|Inhalt der Zwischenablage einfügen" />
            </ToolBar>
            <TextBox
                x:Name="RichEdit1"
                Canvas.Left="0"
                Canvas.Top="24"
                Width="450"
                Height="222"
                AcceptsReturn="True"
                AcceptsTab="True"
                Text=""
                TextWrapping="Wrap" />
            <!--  Non-visual: TActionList Name="ActionList1"  -->
            <!--  Non-visual: TImageList Name="ImageList1"  -->
            <!--  Unknown component: TOpenDialog Name="OpenDialog"  -->
            <!--  Unknown component: TSaveDialog Name="SaveDialog"  -->
            <!--  Unknown component: TSaveDialog Name="SaveDialog1"  -->
        </Canvas>
    </DockPanel>
</Window>
