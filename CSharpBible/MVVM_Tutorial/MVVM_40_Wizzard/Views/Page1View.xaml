<Page
    x:Class="MVVM_40_Wizzard.Views.Page1View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MVVM_40_Wizzard.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="clr-namespace:MVVM_40_Wizzard.ViewModels"
    xmlns:vc="clr-namespace:MVVM.Views.ValueConverter;assembly=MVVM_BaseLib"
    Title="Page1View"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.DataContext>
        <mvvm:Page1ViewModel />
    </Page.DataContext>
    <Page.Resources>
        <vc:Xaml2ElementConverter x:Key="vcXaml2Element" />
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*" />
            <ColumnDefinition Width="60*" />
        </Grid.ColumnDefinitions>
        <GridSplitter />
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <Label Content="Name:" />
            <ListBox
                ItemsSource="{Binding MainOptions}"
                SelectedIndex="{Binding Selection, Mode=OneWay}"
                SelectedItem="{Binding MainSelection}" />
            <Button
                Height="40"
                Margin="10"
                Command="{Binding ClearCommand}"
                Content="Clear" />
        </StackPanel>
        <DockPanel Grid.Column="1">
            <Image
                Width="200"
                DockPanel.Dock="Left"
                Source="{Binding ImageSource}" />
            <FlowDocumentScrollViewer
                Margin="5"
                Document="{Binding Document, Mode=OneWay, Converter={StaticResource vcXaml2Element}}"
                VerticalScrollBarVisibility="Auto" />
        </DockPanel>
    </Grid>
</Page>
