<Window x:Class="PictureDB.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PictureDB.UI.ViewModels"
        Title="PictureDB" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="8">
            <TextBox x:Name="FolderPathTextBox" Width="500" Text="{Binding FolderPath, Mode=TwoWay}"/>
            <Button Content="Browse" Margin="8,0,0,0" Command="{Binding BrowseCommand}"/>
            <Button Content="Analyze" Margin="8,0,0,0" Command="{Binding AnalyzeCommand}" CommandParameter="{Binding FolderPath}"/>
        </StackPanel>

        <ListView ItemsSource="{Binding Results}" Grid.Row="1" Margin="8">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="File" DisplayMemberBinding="{Binding FilePath}" Width="300"/>
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="150"/>
                    <GridViewColumn Header="Score" DisplayMemberBinding="{Binding Score}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Text="{Binding Status}" Grid.Row="2" Margin="8"/>
    </Grid>
</Window>