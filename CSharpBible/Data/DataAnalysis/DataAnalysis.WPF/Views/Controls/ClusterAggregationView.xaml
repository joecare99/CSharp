<UserControl x:Class="DataAnalysis.WPF.Views.Controls.ClusterAggregationView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:sys="clr-namespace:System;assembly=mscorlib"
 xmlns:plot="clr-namespace:DataAnalysis.WPF.Views.Converters"
 mc:Ignorable="d">
    <UserControl.Resources>
        <plot:PlotConverter x:Key="PlotXConverter"/>
        <plot:SizeByCountConverter x:Key="SizeByCountConverter"/>
        <!-- neu: zentrierte Konverter -->
        <plot:PlotXCenteredConverter x:Key="PlotXCenteredConverter"/>
        <plot:PlotYCenteredConverter x:Key="PlotYCenteredConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Legende / Rohdaten-Tabelle -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Rohdaten" FontWeight="Bold" Margin="0,0,0,4" FontSize="12"/>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding RawPoints}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          CanUserSortColumns="True"
                          CanUserResizeColumns="False"
                          CanUserReorderColumns="False"
                          SelectionMode="Single"
                          Margin="0"
                          FontSize="10">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Height" Value="18"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="X" Binding="{Binding X, StringFormat={}{0:F2}}" Width="*"/>
                        <DataGridTextColumn Header="Y" Binding="{Binding Y, StringFormat={}{0:F2}}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Plot area -->
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>
                <!-- Plot host fills cell -->
                <Grid x:Name="PlotHost" Grid.Row="1" Grid.Column="1" Background="#0E000000">
                    <!-- raw points overlay -->
                    <ItemsControl ItemsSource="{Binding RawPoints}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource PlotXConverter}">
                                            <Binding Path="X"/>
                                            <Binding Path="DataContext.MinX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding Path="DataContext.MaxX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding ElementName="PlotHost" Path="ActualWidth"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Canvas.Top">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource PlotXConverter}">
                                            <Binding Path="Y"/>
                                            <Binding Path="DataContext.MaxY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding Path="DataContext.MinY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding ElementName="PlotHost" Path="ActualHeight"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Ellipse Width="4" Height="4" Fill="#404040" Stroke="#808080" StrokeThickness="0.5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- cluster bubbles overlay (Mittelpunkt = Datenpunkt) -->
                    <ItemsControl ItemsSource="{Binding Points}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource PlotXCenteredConverter}">
                                            <Binding Path="X"/>
                                            <Binding Path="DataContext.MinX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding Path="DataContext.MaxX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding ElementName="PlotHost" Path="ActualWidth"/>
                                            <Binding Path="Count"/>
                                            <Binding Path="DataContext.MaxCount" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Canvas.Top">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource PlotYCenteredConverter}">
                                            <Binding Path="Y"/>
                                            <Binding Path="DataContext.MinY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding Path="DataContext.MaxY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            <Binding ElementName="PlotHost" Path="ActualHeight"/>
                                            <Binding Path="Count"/>
                                            <Binding Path="DataContext.MaxCount" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Ellipse StrokeThickness="1">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="#7F2D89EF"/>
                                    </Ellipse.Fill>
                                    <Ellipse.Stroke>
                                        <SolidColorBrush Color="#2D89EF"/>
                                    </Ellipse.Stroke>
                                    <Ellipse.Width>
                                        <MultiBinding Converter="{StaticResource SizeByCountConverter}">
                                            <Binding Path="Count"/>
                                            <Binding Path="DataContext.MaxCount" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                        </MultiBinding>
                                    </Ellipse.Width>
                                    <Ellipse.Height>
                                        <MultiBinding Converter="{StaticResource SizeByCountConverter}">
                                            <Binding Path="Count"/>
                                            <Binding Path="DataContext.MaxCount" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                        </MultiBinding>
                                    </Ellipse.Height>
                                    <Ellipse.ToolTip>
                                        <StackPanel>
                                            <TextBlock Text="{Binding X, StringFormat='X={0:F1}'}"/>
                                            <TextBlock Text="{Binding Y, StringFormat='Y={0:F1}'}"/>
                                            <TextBlock Text="{Binding Count, StringFormat='Count={0}'}"/>
                                        </StackPanel>
                                    </Ellipse.ToolTip>
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Opacity" Value="0.8"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Fill">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="#FF2D89EF"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Stroke">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="#FF1B5E9E"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Canvas x:Name="AxisOverlay" IsHitTestVisible="False">
                        <!-- Horizontale Y-Ticks links -->
                        <ItemsControl ItemsSource="{Binding YTicks}" IsHitTestVisible="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left" Value="0"/>
                                    <Setter Property="Canvas.Top">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource PlotXConverter}" ConverterParameter="-6.5">
                                                <Binding Path="Value"/>
                                                <Binding Path="DataContext.MaxY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                <Binding Path="DataContext.MinY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                <Binding ElementName="PlotHost" Path="ActualHeight"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                                        <TextBlock Text="{Binding Label}" FontSize="10" Foreground="#808080" Width="45" TextAlignment="Right"/>
                                        <Rectangle Width="6" Height="1" Fill="#606060" Margin="4,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- Vertikale X-Ticks unten -->
                        <ItemsControl ItemsSource="{Binding XTicks}" IsHitTestVisible="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource PlotXConverter}" ConverterParameter="-4">
                                                <Binding Path="Value"/>
                                                <Binding Path="DataContext.MinX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                <Binding Path="DataContext.MaxX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                <Binding ElementName="PlotHost" Path="ActualWidth"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Canvas.Top" Value="{Binding ElementName=PlotHost, Path=ActualHeight}"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,2,0,0">
                                        <Rectangle Width="1" Height="6" Fill="#606060"/>
                                        <TextBlock Text="{Binding Label}" FontSize="10" Foreground="#808080" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!-- Null-Linie X=0 -->
                        <Rectangle Width="1" Fill="#707070" Visibility="{Binding HasZeroX, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Height="{Binding ElementName=PlotHost, Path=ActualHeight}" >
                            <Canvas.Left>
                                <MultiBinding Converter="{StaticResource PlotXConverter}">
                                    <Binding Source="0"/>
                                    <!-- wird ignoriert, wir geben 0 explizit -->
                                    <Binding Path="DataContext.MinX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding Path="DataContext.MaxX" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding ElementName="PlotHost" Path="ActualWidth"/>
                                </MultiBinding>
                            </Canvas.Left>
                        </Rectangle>
                        <!-- Null-Linie Y=0 -->
                        <Rectangle Height="1" Fill="#707070" Visibility="{Binding HasZeroY, Converter={StaticResource BooleanToVisibilityConverter}}"
                               Width="{Binding ElementName=PlotHost, Path=ActualWidth}">
                            <Canvas.Top>
                                <MultiBinding Converter="{StaticResource PlotXConverter}">
                                    <Binding Source="0"/>
                                    <Binding Path="DataContext.MaxY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding Path="DataContext.MinY" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                    <Binding ElementName="PlotHost" Path="ActualHeight"/>
                                </MultiBinding>
                            </Canvas.Top>

                        </Rectangle>
                    </Canvas>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
