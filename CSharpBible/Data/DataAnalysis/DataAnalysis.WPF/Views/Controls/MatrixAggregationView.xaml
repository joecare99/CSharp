<UserControl x:Class="DataAnalysis.WPF.Views.Controls.MatrixAggregationView"
	 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	 mc:Ignorable="d">
	<UserControl.Resources>
		<Style x:Key="RotatedColumnHeaderStyle" TargetType="DataGridColumnHeader">
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="ContentTemplate">
				<Setter.Value>
					<DataTemplate>
						<Border Padding="4" RenderTransformOrigin="0.5,0.5">
							<Border.LayoutTransform>
								<RotateTransform Angle="-90"/>
							</Border.LayoutTransform>
							<TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"/>
						</Border>
					</DataTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
		<ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Grid.Row="1">
			<DataGrid ItemsSource="{Binding TableView}" AutoGenerateColumns="True" IsReadOnly="True"
					 ColumnHeaderStyle="{StaticResource RotatedColumnHeaderStyle}" ColumnHeaderHeight="140"/>
		</ScrollViewer>
	</Grid>
</UserControl>