# Task T-SharedSkills-001 - Create global test coverage skill

## Status

Done

## Parent

- Backlog Item `BI-SharedSkills-001` - `Establish reusable global agent skills`

## Goal

Provide a reusable shared skill that runs tests for a .NET test project with code coverage and reports per-class coverage details including uncovered lines.

## Scope

- Create a shared PowerShell script in the global tools/skills path
- Run `dotnet test` with XPlat coverage collector
- Parse `coverage.cobertura.xml`
- Output class name, file path, coverage percentage, and uncovered line numbers
- Provide JSON output mode for machine-consumable agent workflows
- Add usage documentation

## Implementation

- Script path: `C:\Projekte\CSharp\Tools\Skills\TestCoverage\Invoke-TestProjectCoverage.ps1`
- Documentation path: `C:\Projekte\CSharp\Tools\Skills\TestCoverage\README.md`

## Validation

- Confirm script supports parameter help and command binding
- Confirm script fails clearly when no coverage report exists
- Confirm output includes required class/file/coverage/uncovered-line fields

## Notes

- The skill is placed in the shared global tools folder so other agents can discover and reuse it.
- Coverage evaluation is line-based and uses Cobertura report data generated by `XPlat Code Coverage`.
