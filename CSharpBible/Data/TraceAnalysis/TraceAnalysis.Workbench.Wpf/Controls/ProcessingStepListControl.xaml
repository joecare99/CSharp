<UserControl x:Class="TraceAnalysis.Workbench.Wpf.Controls.ProcessingStepListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <GroupBox Header="Processing Steps">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        Margin="0,0,0,8">
                <Button Width="90"
                        Margin="0,0,8,0"
                        Command="{Binding DataContext.ProcessingEditor.AddStepCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Content="Add Step" />
                <Button Width="110"
                        Command="{Binding DataContext.ProcessingEditor.RemoveStepCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Content="Remove Step" />
            </StackPanel>

            <ListBox ItemsSource="{Binding Steps}"
                     SelectedItem="{Binding SelectedStep}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#DCEEFF" />
                            <Setter Property="BorderBrush" Value="#4A90E2" />
                            <Setter Property="BorderThickness" Value="1" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8"
                            Padding="8"
                            BorderBrush="Gainsboro"
                            BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                        <Setter Property="BorderBrush" Value="#4A90E2" />
                                        <Setter Property="Background" Value="#F3F8FE" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBox FontWeight="Bold"
                                     Text="{Binding StepId, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Margin="0,6,0,0"
                                     Text="{Binding OperationName, UpdateSourceTrigger=PropertyChanged}" />
                            <CheckBox Margin="0,8,0,0"
                                      Content="Enabled"
                                      IsChecked="{Binding IsEnabled}" />
                            <ItemsControl Margin="0,8,0,0"
                                          ItemsSource="{Binding Outputs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,2,0,2">
                                            <TextBlock Width="80"
                                                       FontWeight="SemiBold"
                                                       Text="{Binding OutputRole}" />
                                            <TextBlock Text="{Binding ChannelName}" />
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </GroupBox>
</UserControl>
